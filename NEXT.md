- Have everything like this: Clone this folder, then health check is not required by user at any moment. The make should itself give output as deployed or similar when everything has started healthily. Immediately after the "make start-local" or "make start" (with current docker Rust builds) exits with status, the admin should run "make config" optional to deviate from defaults, next immediately register, then upload binaries by contestants, then monitor status, then leaderboard with rankings contestant_id, name, correctness_pct, composite, current_tps, failure_reason, orders_sent, total_fills and other things required/missing, check PLAN.md and think what else fits (refreshing this should give latest data), then live data endpoint. Both leaderboard and live data should have status of each contestant and full test status: completed or running (all uploaded binaries have finished running). On finished no new data needs to be emitted by sse.

- On "make e2e" the above given workflow must be emulated thoroughly. All leaderboard states, their changes, etc. Use Rust or python or zx and js if bash makes it too complex to do complete validation of curl outputs.

- Sample must be configured with correct by default, prefilled, panic_10s, slow_submit and randomize_price. This is used to test leaderboard and rankings work correctly. At the end of leaderboard, default must be 1st, panic_10s must be last, and everything else must be in-between verified by the score they get.